defaults:
  - multi_stream: TrendSeasonal
  - embedding: Patch
  - encoder: Transformer
  - decoder: Linear
  - _self_
use_multi_stream: True
share:
  embedding: True
  encoder: True
  decoder: True
# decoder:
  # init_parameters: True
use_norm: True
learning_rate: 1e-2